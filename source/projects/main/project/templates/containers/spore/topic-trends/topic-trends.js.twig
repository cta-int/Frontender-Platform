jQuery(function($) {
    $(".topic-trends").slick({
        dots: false,
        infinite: true,
        centerMode: true,
        variableWidth: false,
        mobileFirst: true,
        prevArrow: $('.carousel-previous'),
        nextArrow: $('.carousel-next'),
        responsive: [
            {
                breakpoint: 600,
                settings: {
                    variableWidth: true
                }
            }
        ]
    });

    var carousselHeight = 0;
    jQuery(function($) {
        setTimeout(function() {
            $(".slick-track").each(function() {
                carousselHeight = $(this).height() > carousselHeight ? $(this).height() : carousselHeight;
            });
            $(".trend").each(function() {
                // $(this).css("height", carousselHeight);
            });
        }, 100);
    });
});
