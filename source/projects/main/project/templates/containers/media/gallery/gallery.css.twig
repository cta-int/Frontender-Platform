<style type="text/css" media="screen">
    .gallery {
        visibility: hidden;
        pointer-events: unset;
        position: absolute;
        left: 0;
        right: 0;
        top: 0;
        min-height: 100vh;
        z-index: 1;
    }
    .gallery--active {
        visibility: visible;
        pointer-events: unset;
    }
    .gallery__cloak {
        position: fixed;
        top: 0; right: 0; bottom: 0; left: 0;
        background: rgba(0,0,0,.72);
        z-index: -1;
    }
    .stage {
        width: 80vw;
        max-width: 800px;
        margin: auto;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }
    .stage__cue {
        flex: 1 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }
    .stage__toggle {
        text-align: right;
        flex: 0 0;
    }
    .stage__controls .button:first-child {
        float: left;
    }
    .stage__controls .button:last-child {
        float: right;
    }

    .actor {
        background: #000;
        width: 80vw;
        max-width: 800px;
        margin: 0 auto;
    }
    .actor__media-wrapper {
        width: 80vw;
        height: 50vw;
        max-width: 800px;
        max-height: 500px;
    }
    .actor__media {
        display: block;
        position: relative;
        z-index: 1;
        height: 100%;
        max-height: 100%;
        max-width: 100%;
        margin: auto;
    }
</style>