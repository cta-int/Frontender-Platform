{% import "macros/template.html.twig" as helper %}
{% set locales = {'en-GB': 'en', 'fr-FR': 'fr', 'es-ES': 'es', 'pt-PT': 'pt'} %}

<{{ config.container_tag|default('div') }}{{ helper.class_attribute(config, 'ctx-item__date')|raw }}{{ helper.container_id_attribute(frontender, 'frontender')|raw }}>
    <div class="fld">
        <label for="ctx-item__date" class="fld-label">{{ labels.field_label|t(locales)|default('Region'|i18n) }}</label>
        <select id="ctx-item__date" name="upcoming" class="placeholder-visible">
            <option value="">{{ 'Any'|i18n }}</option>
            <option value="true" {% if config.selected == true or query.upcoming == "true" %}selected="selected" {% endif %}>{{ 'Upcoming'|i18n }}</option>
            <option value="false" {% if config.selected == false or query.upcoming == "false" %}selected="selected" {% endif %}>{{ 'Passed'|i18n }}</option>
        </select>
    </div>
</{{ config.container_tag|default('div') }}>
