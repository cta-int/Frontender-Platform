{% import "macros/template.html.twig" as helper %}
{% set locales = {'en-GB': 'en', 'fr-FR': 'fr'} %}

<{{ config.container_tag|default('div') }}{{ helper.class_attribute(config, 'ctx-item__submit')|raw }}{{ helper.container_id_attribute(frontender, 'frontender')|raw }}>
    <button class="button button--wide" type="submit">{{ labels.label|t(locales)|default('Search'|t(locales)) }}</button>
</{{ config.container_tag|default('div') }}>
