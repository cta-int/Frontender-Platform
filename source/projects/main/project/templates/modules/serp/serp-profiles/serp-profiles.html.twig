{% set locales = {'en-GB': 'en', 'fr-FR': 'fr', 'es-ES': 'es', 'pt-PT': 'pt'} %}

{% if 'profile' in query.type or not query.type %}
    <section class="trailer--giant">
        <h2 class="title title--section  |  fld--label  |  leader  |  trailer  |  gutters">
            {% if model.profilesTotal == 1 %}
                {{ model.profilesTotal }} {{ 'Result in profiles'|i18n }}
            {% else %}
                {{ model.profilesTotal }} {{ 'Results in profiles'|i18n }}
            {% endif %}
        </h2>
        {% if model.profilesTotal > 0 %}
            <div data-serp-profiles>
                {% include 'modules/serp/serp-profiles/serp-profiles-list-with-load-more.html.twig' %}
            </div>
        {% else %}
            {% if query.q %}
                <p class="leader trailer gutters">{{ 'No profiles found for'|i18n }} &ldquo;{{ query.q }}&rdquo;</p>
            {% else %}
                <p class="leader trailer gutters">{{ 'No profiles found'|i18n }}</p>
            {% endif %}
        {% endif %}
    </section>
{% endif %}
