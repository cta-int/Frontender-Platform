{% set locales = {'en-GB': 'en', 'fr-FR': 'fr'} %}

<{{ config.container_tag|default('h1') }} class="{{['title title--section  |  gutters', config.container_class]|join('  |  ') }}">
    {{ config.container_title|t(locales) }}
</{{ config.container_tag|default('h1') }}>
