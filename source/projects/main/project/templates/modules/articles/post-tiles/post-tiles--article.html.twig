{% import "macros/template.html.twig" as helper %}

<{{ config.container_tag|default('div') }}{{ helper.class_attribute(config, 'grid grid--tiles')|raw }}{{ helper.container_id_attribute(frontender, 'frontender')|raw }}>
    {% include "partials/article/post-tile/articles.html.twig" with {labels: labels} %}
</{{ config.container_tag|default('div') }}>
